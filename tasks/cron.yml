- name: install cron job (ansible-pull)
  cron:
    user: ansible
    name: "ansible provision"
    minute: "*/1"
    job: "/usr/bin/ansible-pull -o -U https://github.com/sanath111/ansible_pull_test.git > /var/log/ansible_cron.log"
